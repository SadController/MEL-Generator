'use strict';
const fs = require('node:fs');
const path = require('node:path');
const assert = require('node:assert/strict');
const root=path.resolve(__dirname,'..');
const reports=['dev-qa','restart-qa-fixed','packaged-qa','installed-qa','portable-final-qa'];
const results=Object.fromEntries(reports.map(name=>{
  const report=JSON.parse(fs.readFileSync(path.join(root,'test-output',name,'integration.json'),'utf8'));
  assert.equal(report.success,true,name);
  return [name,report];
}));
const installation=JSON.parse(fs.readFileSync(path.join(root,'test-output/installation.json'),'utf8').replace(/^\uFEFF/,''));
for(const k of ['installedQaPassed','executableRemoved','shortcutRemoved','registryEntryRemoved'])assert.equal(installation[k],true,k);
const size=name=>fs.statSync(path.join(root,'release',name)).size.toLocaleString('en-US');
const pdfUrl=results['portable-final-qa'].checks.find(c=>c.url)?.url;
const report=`# MEL Generator 1.0.0 — отчёт о проверках

Дата: 16.09.2026. Проверена итоговая сборка Windows x64.

## Среда

- Windows 10 Enterprise LTSC, версия 10.0.19044, x64.
- Electron 44.3.0, electron-builder 26.15.3, Node.js ${process.version} для сборки.
- Интерфейс: собственные HTML/CSS/JavaScript; внешних сервисов нет.
- Исходный MMEL: FAA A320 Rev. 32, 30.07.2025, 472 страницы.

## Логика и каталог

Все 8 автоматических тестов пройдены. Проверены:

- 53 записи, 29 профилей и 47 английских вариантов условий. Идентификаторы,
  пути EFB, пункты и страницы MMEL и ограничения совпадают с согласованными
  исследовательскими JSON. Тексты карточек сверены с сохранённым английским
  извлечением используемых пунктов MMEL.
- Все 24 857 наборов: 53 одиночных; 1 378 пар; 23 426 троек.
  Результаты совпадают с независимым реестром 89 запрещённых пар и двух
  дополнительных минимальных запрещённых троек.
- Допустимы 53 одиночных, 1 289 пар и 19 311 троек; заблокированы 89 пар и
  4 115 троек. Варианты негерметизированного полёта отсутствуют.
- Для каждого допустимого набора выбор альтернативной строки совпадает с
  исходными правилами. Проверены отдельные примеры двух AP, двух APU loops,
  двух DCDU, двух TAT, CPC/FCU, тормозов и сочетаний трёх обогревателей.
- Выполнен полный цикл выдачи каждого пула. Комбинации не повторяются,
  переключение самолёта и количества сохраняет соответствующую историю.
  На границе циклов нет повтора. Новый экземпляр генератора начинает новый цикл.
- Ошибочные запросы отклоняются; настройки восстанавливаются из файла,
  повреждённые настройки заменяются стандартными. Сценарии не сохраняются.
- Вложенный PDF побайтно совпадает с принятым исходником.

## Приложение и готовые EXE

Успешно завершены проверки исходной, повторно запущенной, упакованной,
установленной и перенесённой Portable-версий. Проверялись реальные окна и
renderer Electron, а не только отдельный модуль генератора.

- A319/1, A320/2, A321/3: правильные подписи и количество карточек.
- To failures создаёт результат; Setup/Failures и Back to main menu сохраняют
  текущий результат и выбор. 54 последовательные генерации через интерфейс
  проходят границу цикла одиночных отказов без соседних повторов.
- Настройки A319/1 восстановлены после перезапуска; вкладка Failures снова
  заблокирована до первого результата — старая история не загружается.
- Все проверки генерации, вкладок и PDF выполняются с отключённой сетью
  в сессии Electron. Приложение не использует локальный HTTP-сервер.
- PDF открыт в отдельном окне на странице из выбранной карточки. При закрытии
  просмотрщика результат сохранён. Пример последней Portable-проверки:
  ${pdfUrl}.
- Снимки интерфейса и PDF просмотрены. Проверены обычное и узкое окно;
  горизонтального переполнения и русских надписей в интерфейсе нет.
- Проверены context isolation и отсутствие require/process у renderer;
  preload предоставляет только четыре предусмотренные операции.
- Упакованные main, QA, UI и JSON побайтно сверены с исходными файлами.
- Portable EXE скопирован и запущен из папки «Проверка переносимой сборки»;
  профиль также находится в пути с кириллицей и пробелами.
- NSIS установлен в отдельную тестовую папку. Код завершения установки: 0.
  Созданы ярлык меню «Пуск» и запись удаления в HKCU. Установленное приложение
  успешно прошло интеграционные проверки.
- Удаление: код 0. Проверено отсутствие установленного EXE, ярлыка и записи
  удаления. Тестовая установка удалена, обе распространяемые сборки сохранены.
- Необработанных ошибок интерфейса в итоговых интеграционных проверках нет.

## Размеры и состав

- Portable: ${size('MEL-Generator-1.0.0-portable.exe')} байт.
- Установщик: ${size('MEL-Generator-Setup-1.0.0.exe')} байт.
- Оба EXE включают среду Electron и FAA PDF. Отдельный Node.js, Python или
  браузер для пользователя не требуется. Лицензии Electron/Chromium включены.
- EXE не подписаны сертификатом издателя. SHA-256 файлов — в SHA256SUMS.txt.

## Границы проверки

- Windows 11 и отдельная чистая виртуальная машина не проверялись.
  Целевые Windows 10/11 x64 не означают проверку всех конфигураций компьютеров.
- Не проводились испытания отказов в Fenix, автоматическое включение отказов,
  проверка маршрута/погоды или повторная оценка применимости по двигателям.
  Это соответствует согласованному объёму проекта.
- Интерфейс проверен встроенным сценарием Electron и просмотром полученных
  снимков. Дополнительно прочитано настоящее окно Portable через Windows UI
  Automation. Внешний инструмент кликов/снимков Windows на этой LTSC выдавал
  ошибку SetIsBorderRequired / unsupported interface, поэтому ручной проход
  всех кнопок этим инструментом не заявляется.
- Ранние тестовые снимки иногда запрашивались до первого кадра и давали
  UnknownVizError. В тестовом сценарии добавлено ожидание кадра; итоговые
  проверки со снимками повторно прошли успешно.

Машиночитаемые результаты и снимки сохранены в test-output/ проекта:
${reports.map(n=>'- '+n+'/integration.json').join('\n')}
- installation.json

Исторические неудачные попытки сохранены для диагностики и не относятся к
итоговой проверке. Исходный архив содержит тесты и инструкции их повторения.
`;
fs.writeFileSync(path.join(root,'release/TEST-REPORT.md'),report);
console.log('All release verification gates passed; report written.');
